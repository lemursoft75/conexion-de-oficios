<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 📌 Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConexionDeOficios"
        tools:targetApi="34">

        <!-- 📌 SplashActivity es la primera pantalla -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 📌 Pantalla de inicio de sesión -->
        <activity android:name=".activities.LoginActivity"/>

        <!-- 📌 Registro -->
        <activity android:name=".activities.RegisterContractorActivity" />
        <activity android:name=".activities.RegisterClientActivity" />

        <!-- 📌 Directorio y detalles de contratistas -->
        <activity android:name=".activities.DirectoryActivity"/>
        <activity android:name=".activities.ContractorDetailActivity"/>

        <!-- 📌 Publicación de anuncios por contratistas -->
        <activity android:name=".activities.PublicationActivity"/>

        <!-- 📌 Configuración (opcional, descomentarlo si se necesita) -->
        <!--<activity android:name=".activities.SettingsActivity"/>-->

    </application>
</manifest>
